{
  "name": "@cursor-skill/poteto-mode-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test check-source-paths.test.mjs && bun test orch watch-pr",
    "test:paths": "node --test check-source-paths.test.mjs",
    "typecheck": "tsc --project tsconfig.json --noEmit --strict"
  },
  "dependencies": {
    "commander": "14.0.0"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "latest"
  }
}
